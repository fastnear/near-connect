{
  "name": "@fastnear/near-wallets",
  "version": "0.7.2",
  "type": "module",
  "license": "MIT",
  "author": "HOT Labs and FastNear",
  "repository": {
    "type": "git",
    "url": "https://github.com/fastnear/near-connect"
  },
  "homepage": "https://github.com/fastnear/near-connect",
  "scripts": {
    "postinstall": "patch-package",
    "build:wallets": "yarn build:hotwallet && yarn build:mnw && yarn build:near-mobile && yarn build:meteor && yarn build:wallet-connect && yarn build:nightly && yarn build:okx",
    "build:wallet-connect": "PACKAGE=wallet-connect vite build",
    "build:near-mobile": "PACKAGE=near-mobile vite build",
    "build:hotwallet": "PACKAGE=hotwallet vite build",
    "build:nightly": "PACKAGE=nightly vite build",
    "build:meteor": "PACKAGE=meteor vite build",
    "build:okx": "PACKAGE=okx vite build",
    "build:mnw": "PACKAGE=mnw vite build"
  },
  "devDependencies": {
    "@fastnear/borsh-schema": "^0.9.11",
    "@fastnear/utils": "^0.9.11",
    "@fastnear/wallet-adapter": "^0.9.11",
    "@here-wallet/core": "^3.4.0",
    "@near-js/crypto": "^2.5.1",
    "@near-js/transactions": "^2.5.1",
    "@noble/curves": "^2.0.1",
    "@noble/hashes": "^2.0.1",
    "@types/node": "^24.9.1",
    "@walletconnect/sign-client": "^2.23.7",
    "borsh": "^2.0.0",
    "patch-package": "^8.0.1",
    "qrcode-generator": "^1.4.4",
    "rollup-plugin-visualizer": "^7.0.1",
    "typescript": "^5.2.2",
    "vite": "^7.2.2"
  }
}
